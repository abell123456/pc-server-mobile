'use strict';

var libc = require('libc');

module.exports = function () {
	return function *(next) {
		var res = this.response;

		yield next;

		if (res.status() === 200 && res.is('js')) {
			res.data(libc(res.data().toString()));
		}
	};
};
